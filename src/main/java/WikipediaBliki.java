import org.eclipse.jetty.server.Authentication.User;
import org.eclipse.jetty.server.Server;
import org.eclipse.jetty.servlet.ServletContextHandler;
import org.eclipse.jetty.servlet.ServletHolder;
/*
import info.bliki.api.*; // needs to be something alone these lines.


public class WikipediaBliki {
	
	public static void main(String[] args) throws Exception {
		String[] listOfTitleStrings = { "Web service" };
		User user = new User("", "", "http://en.wikipedia.org/w/api.php");
		user.login();
		
		String query;
		String topic
	while (true){
			try{
				query = "";
				topic = wikipedia.summary(query);
				break;
			catch {
				wikipedia.exceptions.DisambiguationError as e:
					for i in range(5):
						System.out.println (e.options[i]);
				continue;
				
	for t in topic.split)
			}
			}
		}
		
	}
		
}
*/